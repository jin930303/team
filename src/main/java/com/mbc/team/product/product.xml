<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mbc.team.product.ProductService">

<insert id="inserta">
	insert into product values(product_seq.nextval, #{param1}, #{param2}, #{param3}, #{param4}, 
	#{param5}, #{param6})
</insert>

<select id="outa" resultType="com.mbc.team.product.ProductDTO">
  select * from product
</select>

<select id="detail" resultType="com.mbc.team.product.ProductDTO">
  select itemnum , image1 ,cg_code, scg_code , product , price , dimage from product where itemnum=#{param1}
</select>

<select id="outmainbest" resultType="com.mbc.team.product.ProductDTO">
  select * from product order by itemnum desc
</select>

<select id="outmainnew" resultType="com.mbc.team.product.ProductDTO">
  select * from product order by itemnum asc
</select>

<select id="arbat" resultType="com.mbc.team.product.ProductDTO">
  select * from product where scg_code=#{param1}
</select>

<select id="outd" resultType="com.mbc.team.product.ProductDTO">
  select * from product
</select>

<delete id="deleteProduct" parameterType="int">
   delete from product where itemnum = #{itemnum}
</delete>

<select id="update" resultType="com.mbc.team.product.ProductDTO">
 select * from product where itemnum = #{param1} 
 </select>

<update id="productupdatesave"> 
  update product set image1 = #{param1, jdbcType=VARCHAR}, cg_code = #{param2, jdbcType=VARCHAR}, scg_code = #{param3, jdbcType=VARCHAR}, 
                     product = #{param4, jdbcType=VARCHAR}, price = #{param5, jdbcType=INTEGER}, dimage = #{param6, jdbcType=VARCHAR} 
                     where itemnum = #{param7, jdbcType=INTEGER}
</update>



</mapper>

